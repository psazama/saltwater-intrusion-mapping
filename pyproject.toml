[project]
name = "swmaps"
version = "0.1.0"
dependencies = [
    "dagster",
    "dagster-k8s",
    "dagster-gcp[gcs]",
    "numpy",
    "pandas",
    "rasterio",
    "geopandas",
    "shapely",
    "xarray",
    "scikit-learn",
]

[tool.setuptools]
packages = ["swmaps"]
